import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  userImageContainer: {
    borderColor: '#F35BAC',
    borderWidth: 1,
    padding: 3,
    borderRadius: 50,
  },
  image: {
    height: 50,
    width: 50,
  },
});
export default style;
